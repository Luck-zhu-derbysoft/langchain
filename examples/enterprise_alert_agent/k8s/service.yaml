apiVersion: v1
kind: Service
metadata:
  name: enterprise-alert-agent
  namespace: enterprise-alert
  labels:
    app.kubernetes.io/name: enterprise-alert-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: enterprise-alert-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
